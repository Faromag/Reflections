How did viewing a diff between two versions of a file help you seet he bug that was introduced?
diff compares and outputs the changes 

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
By having the history you could easily return to a previous version or run diff to compare changes

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?
pros would be having only what you want saved when you want it saved
cons would be not having updated work saved automatically

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?
Because git is for handling more code that could and would be changed across multiple files where Google is a bit more primitive treating every file different

How can you use the commands git log and git diff to view the history of files?
git log displays all the versions is a chronological order
git diff can then compare the differences

How might using version control make you more confident to make changes that could break something?
by allowing you to return back to a previous verion before you broke it

Now that you have your workspace set up, what do you want to try using Git for?
comparing programs at work